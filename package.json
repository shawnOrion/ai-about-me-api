{
  "name": "scripts",
  "version": "1.0.0",
  "description": "my-project/\r ├── data/\r │   ├── app_data.json   # Optional: JSON file for seed data\r ├── .env                # Environment variables (template provided)\r ├── index.js            # Main app entry point\r ├── Procfile            # Heroku process file\r ├── package.json        # Node.js package configuration\r └── README.md           # Project setup and instructions",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "elevenlabs": "^1.50.2",
    "express": "^4.21.2"
  }
}
